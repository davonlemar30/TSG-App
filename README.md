# TSG Birth Snapshot

Expo + TypeScript starter structure for TheScorpioGang mobile experiences.

## Note on assets

Default Expo icon/splash files are intentionally not committed in this environment to keep PR diffs text-only.
Add your own PNG assets under `assets/` before building release binaries.
